name: Build and deploy GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
          bundler-cache: true

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Copy raw R Markdown files (excluded from Jekyll processing)
        run: |
          mkdir -p _site/rmd _site/templates
          cp -r rmd/. _site/rmd/
          cp -r templates/. _site/templates/

      - name: Verify rmd files were actually copied
        run: |
          test -f _site/rmd/lab01-inferential-cards.Rmd || {
            echo "::error::rmd/ was not copied into _site/ -- Download links will 404"
            exit 1
          }

      - name: Copy raw R Markdown files (excluded from Jekyll processing)
        # rmd/ and templates/ are excluded in _config.yml so Jekyll never
        # parses their YAML front matter. Copy them into _site/ verbatim so
        # the "Download" links on each lab page still resolve to the real,
        # unmangled .Rmd files.
        run: |
          mkdir -p _site/rmd _site/templates
          cp -r rmd/. _site/rmd/
          cp -r templates/. _site/templates/

      - name: Verify rmd files were actually copied
        # Fails the build loudly instead of silently publishing a site with
        # 404ing "Download" links.
        run: |
          test -f _site/rmd/lab01-inferential-cards.Rmd || {
            echo "::error::rmd/ was not copied into _site/ -- Download links will 404"
            exit 1
          }

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "_site"

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
